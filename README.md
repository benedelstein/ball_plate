# Bob's Donuts website concept

A responsive, single-page website for Bob's Donuts & Pastries. The site uses semantic HTML, custom CSS, and lightweight vanilla JavaScript.

## Run locally

```bash
python3 server.py
```

Then visit `http://localhost:8080`.
